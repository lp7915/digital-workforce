---
name: social-trend-report
description: "[social-trends] 社媒热点周报交付：汇总事件与洞察，生成有来源的 Markdown 和 HTML 草稿，核验报告后通过已授权飞书工具留档，按明确授权使用真实托管服务发布。"
metadata:
  tags: social-trends
  platform: digital-workforce
---

# 周报生成、留档与发布

## 输入
读取当前 run_id 的 events.json、insights.md、质量报告、验收记录及项目报告模板。核对周期与数据版本一致；失败批次、质量限制和未核实信息必须随报告交付。

## 本地交付
1. 按 templates/weekly-report.md 形成 Markdown：一句话结论、周期与质量、四维洞察、事件清单、行动建议、来源与限制。
2. 每个重点结论关联 event_id 与 source_url。图表只能使用本轮数据；没有图片则省略，不虚构截图或图表来源。
3. 使用 references/report-template.html 作为最小结构生成 HTML。所有数据文本必须 HTML 转义，URL 仅允许 http/https，禁止将数据原样作为脚本或 HTML 注入。
4. 本地文件是草稿，没有托管地址不能声称网页可公网访问。

## 外部交付
1. 确认飞书工具真实可用、目标文档或目录已配置且有写权限。未配置时交付本地 Markdown 并列出阻塞项。
2. 调试期先展示报告供确认；只有已配置生产授权或本次明确授权才创建/更新飞书文档及对外发布。创建回执必须保存文档 ID/URL，发布回执必须有可核验页面 URL。
3. 发布依赖客户提供的托管接口、真实工具和凭证引用。不要猜测接口，不将密钥写进网页、文件或报告。
4. 以 run_id 关联 output-manifest.json，保存本地文件、飞书文档、网页地址及每步状态。网络超时先查目标结果，不重复新建文档或重复发布。失败与结果未知分开记录。
5. 未经授权不主动向群聊发送或向其他人分享报告。Memory 中仅沉淀确认结论及链接，不复制全量报告与中间数据。

当前初始化只提供配置及技能，不代表已启用业务调度、持久化人工审批或自动网页托管。

## 最终产物 Validator（必需）

交付前读取 `references/validation-contract.md`，在本轮目录生成严格契约的产物及 SHA256 清单。
运行本 Skill 下的脚本（将路径替换为实际 Skill 安装路径）：

```sh
python3 scripts/validate.py --run-dir runs/<run_id>
```

- 退出码非零：读取 `validation.json.errors`，按文件与错误码修复原产物，重算清单后重新验证。最多修复 2 轮，仍失败则返回具体阻塞项；不得谎称验证通过。
- 退出码为零只证明本地结构检查通过，状态是 `draft_validated`，不等同业务验收或远端发布成功。任何文件变更后必须重跑。
- 随后进行证据复核：逐条检查重点结论与来源是否相符、统计值与本轮数据计算是否一致、是否混淆平台口径、是否将相关性写成因果、是否遗漏样本验收和失败批次。记录到 `semantic-review.md`：结论、证据路径/事件 ID、核算方法、通过/失败/无法核实及原因。无法核实的数据不许标通过，退回修正或明确请求人工验收。
- 语义复核通过且取得必要授权后才可外部交付；保存真实工具回执，并通过真实读取工具回读目标文档/网页核对本轮内容。将结果单独写入 `delivery-receipt.json`。不存在回读能力则为 `unverified`，不能写 `published` 或 `completed`。
- 对用户返回验证结论、未解决项及产物位置，区分“本地草稿已验证”和“外部交付已核验”。这套脚本不负责网络发布，不代表服务端具有强制交付拦截器。
