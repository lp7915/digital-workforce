# 本地飞书 Channel 接入

新建数字员工保存成功后自动发起飞书官方应用创建流程。扫码者确认创建后，服务保存 App ID、App Secret 和创建者标识，再创建独立 MA Agent、运行环境与 Vault，启动飞书 WebSocket Channel。应用属于实际在飞书确认的账号，工作台不能用另一个应用的 CLI 用户 Token 替代此次确认。

## MA 配置

页面右上角「方舟配置」可直接保存 API Key。密钥写入 `data/ma-config.json`（文件权限 `600`），不回显，也不写入浏览器存储。页面配置优先于下述环境变量配置。保存只验证格式，真实有效性在接入 MA 时验证。待接入员工保存后可直接继续接入；已运行的 Channel 替换密钥后需要重启本机服务。

启动服务前设置 `WORKFORCE_MA_API_KEY`，或者将 `WORKFORCE_MA_CONFIG` 指向权限为 `600` 的 JSON 文件：

```json
{ "apiKey": "在本地配置有效的 MA API Key" }
```

配置文件不要提交到 Git。默认 MA 服务为 `https://ark.cn-beijing.volces.com/api/v3`，模型为 `doubao-seed-2-1-pro-260628`。每位员工创建独立的资源，不复用其他 Bot 的运行环境或密钥。若服务启动时没有 MA 配置，可以先完成飞书绑定；配置后重启服务会继续接入已有绑定，不会再创建飞书应用。

## 验收

2026-09-21 已完成真实单聊文本联调：用户发送「你好，请介绍一下自己」，原 Session 在修复业务记忆输入检查点顺序后恢复成功，MA 执行成功并收到飞书发送回执。完整回归测试 1782 项通过。群聊与项目记忆的真实业务验收仍按下述步骤进行。

1. 创建员工，在弹窗使用当前飞书账号扫码确认应用创建。检查飞书确认页面最终列出的权限；模板扩展受飞书平台开放范围影响。
2. 等待状态变成 Channel 已连接。若失败，检查飞书开放平台应用的机器人能力、发布/可用范围、长连接事件订阅和 `im.message.receive_v1`，以及 MA 配置。
3. 创建者在飞书单聊发送文本；工作台接入状态应显示最近收到消息和最近成功回复时间。
4. 群聊测试需要先把机器人加入群，再在工作台登记真实 Chat ID 并关联员工。群内 @机器人发送消息；未关联群不会交给 MA。
5. 项目关联群的消息会读取该项目与员工的记忆文本，作为本次输入的参考上下文。

## 当前边界

- CLI 与工作台共用 `src/employee-runtime.ts`：流式卡片、处理中表情、附件下载、近期历史、引用消息、群聊共享 Session、单聊 OAuth 及凭证生命周期由同一装配提供。群聊仍受工作台员工关联限制，且不注入用户身份；单聊可用范围由飞书应用控制。
- 旧版应用在「查看接入状态」选择补齐权限，为现有 App ID 确认权限后，服务核查实际已开通范围并继续升级。MA Agent 原位更新为原员工配置并向独立 Vault 写入 App Secret；不会删除会话。旧 Session 不自动切换配置，升级后可明确发送 `/new`，下一条消息创建完整配置的新 Session。
- 身份、知识和规则在首次创建 MA Agent 时形成快照；后续工作台修改和版本发布尚未同步到 MA。记忆每次消息读取最新本地内容，此处是上下文注入，不是 MA 原生 Memory 库挂载。
- 工作台任务页的手工任务仍执行本地上下文快照；Channel 的真实消息队列存在独立 Gateway 数据库，尚未汇入该页面。
- App Secret 保存在本地 SQLite 私有表及该员工专属 MA Vault，供 lark-cli 使用 Bot 身份。工作台状态接口不回传密钥。数据库目录和文件使用本机私有权限。
- 应用创建和每个 MA 资源创建都保存检查点。若外部创建返回结果不确定，阻止自动重复创建，应先在对应平台核查资源后修复检查点。已有应用绑定不会因后续启动失败而丢失。
- 启动连接成功代表 WebSocket 已建立；最近回复时间才是成功发送消息的证据。断线自动重连由 Channel SDK 负责，当前状态页尚未提供实时断线诊断。
- 本机工作台监听 `127.0.0.1`，不提供远程访问鉴权；生产部署另行配置。
